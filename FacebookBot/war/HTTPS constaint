L'unico modo per settare il webhook è quello di aggiungere questo codice in web.xml:
<security-constraint>
    <web-resource-collection>
        <web-resource-name>everything</web-resource-name>
        <url-pattern>/*</url-pattern>
    </web-resource-collection>
    <user-data-constraint>
        <transport-guarantee>CONFIDENTIAL</transport-guarantee>
    </user-data-constraint>
</security-constraint>

Successivamente quando impostiamo l'indirizzo del webhook dobbiamo fare riferimento a:
Https://1-dot-%idAppGAE%.appspot.com
in questo modo viene caricato il certificato ssl necessario per usare un webhook